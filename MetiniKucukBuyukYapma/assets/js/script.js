function buyukYap() {
      const metin = document.getElementById("metin").value;
      document.getElementById("sonuc").innerText = metin.toUpperCase();
    }
    function kucukYap() {
      const metin = document.getElementById("metin").value;
      document.getElementById("sonuc").innerText = metin.toLowerCase();
    }